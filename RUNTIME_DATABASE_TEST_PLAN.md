# RUNTIME DATABASE TEST PLAN (KẾ HOẠCH KIỂM THỬ RUNTIME CƠ SỞ DỮ LIỆU)

**Hệ thống:** THE HORI CLICK / Post New (`https://www.thehori.click`)  
**Tiêu chuẩn kiểm thử:** Forensic Runtime & Chaos Engineering Verification  
**Nguyên tắc:** KHÔNG đánh dấu PASS nếu chưa thực hiện chạy test case thực tế tại thời điểm kiểm toán. Mọi kịch bản chưa chạy đều đánh dấu `NOT TESTED`.

---

## Danh Sách 15 Kịch Bản Kiểm Thử (Test Cases Matrix)

| Mã Test | Tên Kịch Bản Kiểm Thử | Điều Kiện Tiền Đề (Precondition) | Thao Tác Thực Hiện (Action) | Kết Quả Mong Đợi (Expected Result) | Hành Vi Code Thực Tế (Actual Code Behavior) | Trạng Thái | Bằng Chứng Mã Nguồn (Evidence) |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| **DB-001** | **Tạo Bài Viết Mới (Create Post)** | Đăng nhập Admin, MongoDB Live, Supabase Live | Gửi POST tạo bài viết từ CMS | Bài viết được ghi vào MongoDB `Post` collection và file `posts/{slug}.json` trên Supabase | `storageService.savePost` gọi `api.createPost` ➔ `Post.create()` và `supabaseStorage.savePostMetadata` | `NOT TESTED` | `storageService.js:161`, `api.js:813`, `supabaseStorage.js:54` |
| **DB-002** | **Cập Nhật Bài Viết (Update Post)** | Bài viết đã tồn tại trên hệ thống | Sửa nội dung bài viết và bấm "Lưu bài viết" | Nội dung mới cập nhật trên MongoDB và Supabase Manifest | `storageService.savePost` gọi `api.updatePost` ➔ `Post.findOneAndUpdate` và ghi đè `posts/{slug}.json` | `NOT TESTED` | `storageService.js:186`, `api.js:844`, `supabaseStorage.js:58` |
| **DB-003** | **Xóa Bài Viết (Delete Post)** | Bài viết đang tồn tại | Bấm "Xóa bài viết" tại `/admin/posts` | Bài viết bị xóa khỏi MongoDB và `posts_manifest.json` | `storageService.deletePost` gọi `api.deletePost` và `supabaseStorage.deletePostMetadata` | `NOT TESTED` | `storageService.js:213`, `api.js:872`, `supabaseStorage.js:107` |
| **DB-004** | **Xóa Sạch LocalStorage (Clear Cache)** | Đã tạo bài viết và sửa hồ sơ | Chạy `localStorage.clear()` trên trình duyệt rồi F5 | Toàn bộ bài viết và nhân sự vẫn được khôi phục từ Cloud | `initializeFromDB()` gọi song song `api.getPosts()` và fetch `posts_manifest.json` từ Supabase | `NOT TESTED` | `storageService.js:70-135` |
| **DB-005** | **Mở Trình Duyệt Mới (New Browser)** | Trình duyệt Chrome đã tạo bài viết A | Mở bài viết A trên Firefox hoặc Tab ẩn danh | Bài viết A hiển thị đầy đủ tiêu đề, ảnh bìa và toàn bộ nội dung HTML | `PostDetailPage.jsx` nhận diện cache rỗng, fetch trực tiếp `/api/posts/:slug` hoặc Supabase CDN | `NOT TESTED` | `PostDetailPage.jsx:58-75` |
| **DB-006** | **Mở Trên Thiết Bị Khác (New Device)** | Laptop tạo bài viết mới | Dùng điện thoại mở trang chủ và trang chi tiết | Bài viết xuất hiện trên trang chủ điện thoại và tải mượt mà | Frontend điện thoại fetch manifest từ Supabase CDN | `NOT TESTED` | `HomePage.jsx:20`, `PostDetailPage.jsx:64` |
| **DB-007** | **Mất Mạng Khi Đang Lưu (Network Offline)** | Đang soạn thảo bài viết, ngắt kết nối Internet | Bấm "Xuất bản bài viết" | Hệ thống báo lỗi mạng rõ ràng, không giả mạo thành công | `api.createPost` và `supabaseStorage` catch error; `storageService` vẫn push vào `localStorage` và báo thành công trên UI (Silent Local Fallback Divergence) | `NOT TESTED` | `storageService.js:199`, `BlogContext.jsx:290` |
| **DB-008** | **MongoDB Mất Kết Nối (DB Failure)** | MONGODB_URI sai hoặc MongoDB Atlas bảo trì | Tạo hoặc sửa bài viết từ CMS | Hệ thống ghi nhận lỗi hoặc lưu vào tầng Cloud CDN dự phòng | `api.createPost` fallback về `memoryStore`, `supabaseStorage` lưu file JSON lên Supabase CDN | `NOT TESTED` | `db.js:71`, `api.js:829` |
| **DB-009** | **Supabase CDN Mất Kết Nối (CDN Failure)** | Supabase Storage bị lỗi 500 hoặc timeout | Tạo bài viết mới | Bài viết vẫn được lưu an toàn trong MongoDB Atlas | `api.createPost` lưu MongoDB thành công; `supabaseStorage` catch error không làm chết ứng dụng | `NOT TESTED` | `storageService.js:195` |
| **DB-010** | **Ghi Đồng Thời 2 Người (Concurrent Update)** | User A và User B cùng mở sửa 2 nhân viên khác nhau | User A và B cùng bấm Lưu cách nhau 1 giây | Cả 2 nhân viên đều được giữ lại trên hệ thống | Pattern Read-Modify-Write trên `staff_manifest.json` không có Lock/ETag, User B có thể ghi đè làm mất thay đổi của User A trên Supabase | `NOT TESTED` | `supabaseStorage.js:155-165` |
| **DB-011** | **Đăng Xuất và Đăng Nhập Lại (Logout/Login)** | Đang đăng nhập tài khoản nhân viên | Đăng xuất rồi đăng nhập lại bằng mật khẩu mới | Phiên làm việc được tạo lại chính xác với đúng phân quyền | `BlogContext.jsx` đọc từ `staffList` để xác thực | `NOT TESTED` | `BlogContext.jsx:213-284` |
| **DB-012** | **Hết Hạn Phiên (Session Expiration)** | Đăng nhập tài khoản CMS | Đóng tab trình duyệt 24h rồi mở lại | Yêu cầu đăng nhập lại nếu phiên hết hạn | Hệ thống lưu `horizon_admin_session: 'true'` vĩnh viễn trong `localStorage`, không có cơ chế timeout/JWT expiry | `NOT TESTED` | `BlogContext.jsx:48`, `storageService.js:683` |
| **DB-013** | **Gọi API Không Có Xác Thực (Unauthorized API)** | Người ngoài không đăng nhập | Gửi lệnh `DELETE /api/posts/post-1` bằng Postman | Backend từ chối với mã lỗi `401 Unauthorized` | Backend không có middleware kiểm tra Token/Header, thực hiện lệnh xóa ngay lập tức | `NOT TESTED` | `server/routes/api.js:872` |
| **DB-014** | **Kiểm Tra Lộ Mật Khẩu (Password Exposure)** | File `staff_manifest.json` trên Supabase | Dùng trình duyệt ẩn danh truy cập URL public `staff_manifest.json` | Không được chứa mật khẩu dạng plaintext | URL công khai trả về toàn bộ trường `password` dạng text | `NOT TESTED` | `supabaseStorage.js:155`, `seedData.js:71` |
| **DB-015** | **File Manifest Bị Lỗi Cú Pháp (Manifest Corruption)** | File `posts_manifest.json` bị lỗi cú pháp JSON | Mở trang chủ website | Trang web tự phục hồi từ MongoDB mà không bị sập | `storageService.initializeFromDB` bọc `try...catch`, fallback về `api.getPosts()` và seedData | `NOT TESTED` | `storageService.js:89`, `App.jsx:38` |
